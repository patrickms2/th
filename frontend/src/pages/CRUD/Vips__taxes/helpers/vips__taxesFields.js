const vips__taxesFields = {
  id: { type: 'id', label: 'ID' },

  tax_name: { type: 'string', label: 'tax_name' },

  tax_rate: { type: 'string', label: 'tax_rate' },
};

export default vips__taxesFields;
