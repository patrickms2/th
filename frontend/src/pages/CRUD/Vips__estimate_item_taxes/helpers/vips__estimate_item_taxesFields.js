const vips__estimate_item_taxesFields = {
  id: { type: 'id', label: 'ID' },

  estimate_item_id: { type: 'int', label: 'estimate_item_id' },

  tax_id: { type: 'int', label: 'tax_id' },

  item_tax: { type: 'string', label: 'item_tax' },
};

export default vips__estimate_item_taxesFields;
