const vips__meeting_membersFields = {
  id: { type: 'id', label: 'ID' },

  user_id: { type: 'int', label: 'user_id' },

  meeting_id: { type: 'int', label: 'meeting_id' },
};

export default vips__meeting_membersFields;
