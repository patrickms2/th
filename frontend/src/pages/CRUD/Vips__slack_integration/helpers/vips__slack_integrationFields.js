const vips__slack_integrationFields = {
  id: { type: 'id', label: 'ID' },

  user_id: { type: 'int', label: 'user_id' },

  access_token: { type: 'string', label: 'access_token' },

  team_id: { type: 'string', label: 'team_id' },

  channel_id: { type: 'string', label: 'channel_id' },
};

export default vips__slack_integrationFields;
