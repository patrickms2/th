const vips__incident_commentsFields = {
  id: { type: 'id', label: 'ID' },

  user_id: { type: 'int', label: 'user_id' },

  incident_id: { type: 'int', label: 'incident_id' },

  parent_id: { type: 'int', label: 'parent_id' },

  comment: { type: 'string', label: 'comment' },

  attachments: { type: 'string', label: 'attachments' },
};

export default vips__incident_commentsFields;
